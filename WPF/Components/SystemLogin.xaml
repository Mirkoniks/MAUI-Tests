<Window x:Class="WPF.Components.SystemLogin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.Components"
        xmlns:vm="clr-namespace:WPF.ViewModels"
        mc:Ignorable="d"
        Title="SystemLogin" Height="450" Width="800">
    <Window.DataContext>
        <vm:LoginViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Width="200" Text="Username:"  Margin="0,50,0,0"/>
        <TextBox  Grid.Row="1"  Width="200" Margin="5"  Text="{Binding Username,Mode=TwoWay}" />
        <TextBlock Grid.Row="2" Width="200" Text="Password:" Margin="5"/>
        <PasswordBox Grid.Row="3" Width="200" Margin="5" Name="password" PasswordChar="*"/>
        <Button Grid.Row="4" Content="Login" Width="200" Margin="0,20,0,0" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=password}"/>
    </Grid>
</Window>
